import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.xxtimeonxx.betteru',
  appName: 'BetterU',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
